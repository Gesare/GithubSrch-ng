export class User {
    // public showListRepo:boolean;
    constructor(public login: string,public name:string, public html_url:string,public avatar_url:string, public public_repos:number,public key:string){
        // this.showListRepo=false;
    }
}
